5.11
log内容截取处理
DIV回车换行的显示
点击多次按钮显示多次的问题
内容长显示错位:word-break:break-all; word-wrap:break-word;

今日工作 20200511
1. 获取并读取遍历报告的日志文件，根据结点索引截取日志中对应部分并在测试步骤中展示。修复重复展示多次的问题。
2. 解决日志文件中换行与编码显示的问题，以及日志展示时div设置格式错位的问题。
明日计划：
1. 根据可视化中选择的结点数量生成对应的示意图，并展示结点包含的部分信息。
问题总结：
1. 临时采用了load的方法避免本地测试时读取日志时跨域提示。

根据结点数量动态生成对应数量按钮https://www.cnblogs.com/szy13037-5/articles/9780782.html  
传递的参数nodelist进行分割https://blog.csdn.net/paul342/article/details/19548659
按钮style语句Assigned expression type string is not assignable to type CSSStyleDeclaration：https://stackoverflow.com/questions/51030396/cost-benefits-of-using-different-ways-to-style-html-elements-with-javascript 改成o.style.cssText
按钮class语句https://zhidao.baidu.com/question/200847997.html 改成o.className
箭头使用无边框按钮代替
生成在指定的div中，document.getElementById("nodegraph").appendChild

动态生成表格： https://www.cnblogs.com/jpfss/p/9510970.html

今日工作 20200512
1. 传递已选结点索引编号，动态生成结点间的连接示意图。查询数据库后在下方显示结点的点击索引、动作和xpath信息表格。
2. 完成动态生成按钮和按钮的class与style设置方法，并将位置指定在对应的div中。以无边框按钮来表示示意图中结点间的箭头。
明日计划：
1. 计算整个图结构中的用例数量，并以类似方式全部进行示意图的展示和部分信息展示。

<div id="nodeInfoTable"和<table id="tableInfo" 的设置，避免table宽度和表格内文字长度有关。
单元格合并：https://www.cnblogs.com/qlqwjy/p/9048317.html \ https://blog.csdn.net/qq_26927285/article/details/79916354

MATCH (n:PageWithES) 
WITH collect(n) as nodes
UNWIND nodes as n
UNWIND nodes as m
WITH * WHERE id(n) < id(m)
MATCH path = allShortestPaths( (n)-[*..4]-(m) )
RETURN path

MATCH p=(a:测试结点)-[*3..5]->(b:测试结点)
RETURN p ORDER BY length(p) limit 20

今日工作 20200513
1. 编写算法获取起止结点选取与不选取的情况下图中所有的路径，同时约束路径首尾结点是否还连接图结构中其他结点来防止查询时间过长。
2. div和表格相关属性的设置，解决表格宽度会随文字长度变化的问题。合并测试用例信息中的部分单元格并获取展示新的属性内容。
明日计划：
1. 修改计算所有路径的算法，同时支持两点之间所有路径计算，作为生成遍历路径功能。

今日工作 20200514
1. 编写生成遍历路径和计算测试用例数量的页面，测试页面间跳转时的结点信息等参数的传递
2. 尝试在生成遍历路径时同时绘制完整的图结构并以不同颜色显示相关结点在其中的位置
明日计划：
1. 修改生成遍历路径的图结构展示代码。整理近期编码时的笔记和论文阅读。

"遗传算法不会受到空间、复杂度等条件的影响……“
模拟退火遗传算法
并把路径相似度加入到适应度函数中
被测程序控制流图和程序遍历图结构
不同遗传算法的对比项目



今日工作 20200515
1. 整理编码时关于网格、参数生成调用、不同组件设置、lombok、关系的获取、测试用例判断部分笔记。
2. 阅读论文，了解一种使用模拟退火遗传算法来生成测试数据的算法。对比被测程序控制流图和程序遍历图结构，学习一种路径相似度的衡量方法。了解到评价算法时需要对比的一些项目。