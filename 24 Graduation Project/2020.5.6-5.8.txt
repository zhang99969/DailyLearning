MATCH (n) WHERE id(n)= 169  MATCH (m) WHERE id(m)=191 match p=(m)-[*3..5]-(n) return p limit 1
MATCH (n) WHERE id(n)= 304 MATCH (m) WHERE id(m)=176 match p=(m)-[*2..2]-(n) return p limit 1


MATCH (n)
WHERE id(n) IN [176,199,198,191]
RETURN n


MATCH (f1:结点) RETURN f1改为
MATCH (f1:结点) MATCH path = (f1)-[]->(f:结点) RETURN path

MATCH (n)
WHERE id(n) IN [176,199,198,191]
MATCH path = (n)-[]->(n) RETURN path

【论文的总结聊天记录粘贴过来：
图数据库中一种使用中间表的方法用来解决可视化中复杂关系结构的论文。
1. 可尝试根据论文中给出的数据结构与思路改进移动应用遍历结果中结点间的关系
2. 论文展示了一种可读性较高的结点可视化风格，可在后续过程中参考
3. 合理规划选择与阅读论文的时间，提高效率


今日工作 20200506
查询资料，编写判断是否构成测试用例的方法：1.根据所选结点中任两个结点间指定长度的路径内包含的结点来判断所选结点是否相连 2.获取每个结点的所有出入度判断是否构成包含所有所选结点的路径 3.显示所选结点之间的所有路径并判断
明日计划
1. 修改方法2中出入度的判断方法
2. 修改测试用例页面显示结点信息方法
问题总结
方法1需要多次选择结点耗费时间，方法3会出现非所选节点造成判断错误，不再使用。

MATCH (n) WHERE id(n)= 304 match (m) where (n)-[]-(m) return id(m)

二维数组 Cannot set property '1' of undefined

全部取消点击再重新点击后，clickednode第一项会出现[empty, "187", "191"]情况导致分析测试用例按钮出错。

今日工作 20200507
1. 修改每个结点出入度的获取方法并进行区分，剔除掉与所选结点无关的结点和连接自身的结点
2. 使用临时变量和修改cypher语句等方法处理由于异步执行导致的变量取值混乱和越界的问题，empty项的处理
明日计划
连接自身节点的判断漏洞修改与论文阅读。
问题总结
多维数组处理的问题可以改成使用自定义类进行处理。

https://blog.csdn.net/enjoying_science/article/details/48103621
https://blog.csdn.net/qq_41587612/article/details/86224564

今日工作 20200508
1. 使用深度优先搜素等方法判断图结构中所选结点是否存在哈密顿路径，实现结点构成测试用例的判断方法
2. 阅读论文，了解可视化技术的分类、方式和基本流程。学习其系统设计过程和不同数据库发挥的作用。
问题总结
有序图中结点的点击顺序不同时判断结果不同问题的解决，等待异步调用结束的解决。