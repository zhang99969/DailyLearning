10.13-10.18
老师好。这是我本学期第8周10月13日至18日的总结：
1.完成的学习任务/工作任务
    (1)、完成最近半年的几份入党积极分子思想汇报，联系入党联系人填写积极分子考察记录并提交。
    (2)、处理Elements.yml文件的转义字符和缩进。对比其中clickedElementsList、已经生成的Neo4j图结构和实际遍历流程的控件，发现clickedElementsList正如英文名所示是点击的控件的顺序，若图结构的节点代表App页面名称则还需要读取更多数据。
    (3)、将clickedElementsList中的所有控件和属性读取并存放到包含Elements自定义类的Map<Integer,Elements>中，替代原来只包含控件内文本内容的Map<Integer,String>，后续可根据AppCrawler对输出文件格式的修改进行适配。
    (4)、对比配置文件中填写的指定遍历路径和输出的freemind.mm文件，具体表示相同但后者不包含重复的控件内容。
    (5)、根据研究内容选择部分并修改初始条件，完成一份本科毕业设计任务书的主要内容和目标。
2.所学知识
    (1)、Elements.yml中reqDom和resDom由<hierarchy>构成，可据此进行读取其内容。
    (2)、阅读一些Neo4j的论文，主要包括知识图谱的研究等，了解其作为项目的流程。对于测吧而言则需要在图结构基础上实现关于软件测试的用例生成、多个图结构的对比等更多功能。
3.下周工作
    完善关于类的代码。阅读论文，准备并完成开题报告有关内容。


10.8-10.11
老师好。这是我本学期第7周10月8日至11日的总结：
1.完成的学习任务/工作任务
    (1)、解析AppCrawler生成的elements.yml文档并进行读取，获得遍历过程中页面实际的跳转流程。
    (2)、使用图数据库对每一个页面创建节点，同时考虑到返回重复页面时等情况下不创建节点。
    (3)、将节点按照实际跳转流程建立连接关系。
2.所学知识
    (1)、图数据库Neo4j中Cypher查询语言的具体使用，使用Java语句生成图数据库后可在浏览器中进行简单的实时调试。
    (2)、节点以及关系是否已存在的判断方法及优化。建立关系连接时的节点顺序的获得方法。
3.遇到的问题及解决
    (1)、使用Java操作图数据库时，需要其在事务中运行。数据库shutdown语句的位置也会影响事务是否成功提交，否则会回滚。
    (2)、判断节点是否已存在后，根据节点出现顺序建立连接时会导致漏掉重复节点与其他节点之间关系，改为根据elements.yml中的map顺序建立并添加节点重复的标识。
    (3)、判断关系是否已存在，使用了获取节点的单个关系与所有关系的方法，其中使用前者方法会在所获取节点存在多个关系时出错并造成无法判断，后改为使用后者方法。
4.下周工作
    页面跳转流程中的页面名称的获取与表示方法；页面中控件的读取与表示。

